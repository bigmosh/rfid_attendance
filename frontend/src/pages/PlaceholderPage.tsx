export function PlaceholderPage({ title }: { title: string }) {
  return <section className="placeholder"><h2>{title} management will be available in the next phase.</h2><p>This Stage 1 dashboard is read-only and displays live attendance data.</p></section>;
}
